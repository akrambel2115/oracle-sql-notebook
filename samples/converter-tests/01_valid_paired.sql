-- oracle-sql-notebook: {"schemaVersion":1,"metadata":{"connectionAlias":"dev"}}
-- %% [sql] {"metadata":{}}
select user, systimestamp from dual;

-- %% [markdown] {"metadata":{}}
-- # Notes
-- This is a markdown cell stored as SQL comments.

-- %% [sql] {"metadata":{"name":"query-2"}}
select 1 as one from dual;

